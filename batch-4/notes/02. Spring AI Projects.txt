In order to develop our applications by using AI, first we need to understand about a company called Open AI. We all come to know the power of AI through Open AI company.
 
===================
What is Open AI ?
===================

=> OpenAI is an AI research and development company that builds AI systems.

=> This OpenAI company helping the people to work, learn, and create more AI systems effectively.

=> It has several AI models to solve real world problems like Text, Images, Audio, Embeddings.

=> It got popular because of its product that is 'ChatGPT'.

=> AI powered chatbot that answers questions and generate text/images based on input. 

=> ChatGpt is browser based tool for individual work flows...


==================
What OpenAI does?
==================

=> OpenAI develops advanced AI models such as:

1) GPT Models -> Powerful language models used in apps, websites and tools. - Generative Pre-trained Transformer

2) DALL-E -> create images from text prompts

3) Whisper -> Converts speech to text

=> OpenAI company exposing their LLMs as REST APIs.

Note: In our application we can integrate OPENAI provided APIs and we can complete the task without developing our own models.


===============
What is LLM ?
===============

=> LLM stands for Large Language Model.  (Model means a Project)

=> LLM means an AI model which got trained with massive data (books, websites, articles, code..)

=> After training got completed, LLM model understand and generates human language.

=> Several companies developed and released LLMs into market.
   Below are the famous LLMs available

	OPEN AI Company ----------------> GPT 4.0/ GPT-4/ GTP-5 etc

	Google Company -----------------> GEMINI

	META ------------------> LLaMA

	Anthropic -------------> Claude


====================
What is Spring AI ?
====================	

=> Spring AI is a spring framework project.

=> Spring AI that helps java developers to build AI-Powered applications easily using existing LLMs.

=> Spring AI is a framework for integrating AI into Spring applications.

=> It simplifies adding generative AI using Spring's modular, POJO-based design

=> Spring AI provides abstractions, starters and integrations to connect your spring applications with AI models and AI services.


		Spring AI = Spring Boot + AI Models

=> Spring AI wraps complex AI model APIs (OpenAI, Ollama, Bedrock) in familiar Spring interfaces.

===========================
Spring AI Project Setup
===========================		
I) Choose LLM Model

Step-1 : OpenAI Account setup

	URL : https://platform.openai.com/

Step-2 : Generate API key to access OpenAI LLMs	
Create an API Key > 
Name: My Test Key
Project : Default project
Permissions : All
> Create secret key

Key : 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



Step-3 : Create Spring Boot application with below dependencies

		- spring web
		- thymeleaf starter
		- lombok
		- devtools
		- open ai starter - spring-ai-starter-model-openai


Step-4 : Configure below properties in "application.yml" file 

```
spring:
  ai:
    openai:
      api-key: <YOUR_API_KEY>
      chat:
        options:
          model: gpt-4o
          max-tokens: 100
```

spring:
  application:
    name: spring-ai-app
  ai:
    openai:
      api-key: <YOUR_API_KEY>
      chat:
        model: gpt-4o		gpt-5/ gpt-5-nano/mini   spring.ai.openai.chat.max-completion-tokens=200
        max-tokens: 100


Step-5 : Create a service class to communicate with "Open AI" model using "Spring AI components".

Step-6 : Create a Controller class to handle request and response

Step-7 : Create view page using thymeleaf.



Note: In Spring AI, max-tokens is used for standard non-reasoning models like GPT-4o, while max-completion-tokens is required for reasoning models like OpenAI's o1 and o3 series to budget both visible output and hidden reasoning tokens

Note: ** Chat Client: Inject and use the standard ChatClient builder provided in Spring AI to execute prompts against your chosen GPT model


For Capgemini - US hosted
spring.ai.openai.base-url=https://openai.generative.engine.capgemini.com


Example # 1

@RestController
@RequestMapping("/api")
public class ChatRestController {

    private final ChatClient chatClient;

    public ChatRestController(ChatClient.Builder chatClientBuilder) {
        this.chatClient = chatClientBuilder.build();
    }


    @GetMapping("/chat")
    public String chat(@RequestParam("message") String message) {
        return chatClient.prompt(message).call().content();
    }
}


CTRL + SHIFT + T ---> ChatClient / DefaultChatClient
CTRL + SHIFT + T ---> ChatModel
CTRL + SHIFT + T ---> OpenAIChatModel


logging.pattern.console=%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n


Refer: https://developers.openai.com/api/docs/pricing


What is your name and which model are you using?


======================== Open AI Service class================================

@Service
public class OpenAIService {

	private ChatClient chatClient;

	// state less client
	public OpenAIService(ChatClient.Builder builder) {
		this.chatClient = builder.build();
	}


	public String askAnything(String question) {
	     return	chatClient.prompt()
				  .user(question)
				  .call()
				  .content();
}



Note: ** In Spring AI, prompt() initiates the fluent request API chain (or overrides the full request context), while user() explicitly sets the actual message content representing the end-user's input


========================== Open AI Controller =================================

package com.cognizant.controllers;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestParam;

import com.cognizant.services.OpenAIService;

@Controller
public class OpenAIController {

	@Autowired
	private OpenAIService openAiService;

	@GetMapping("/")
	public String index() {
		return "index";
	}

	@PostMapping("/askAnything")
	public String handleAskAnything(@RequestParam String question, Model model) {

		String answer = openAiService.askAnything(question);

		model.addAttribute("question", question);
		model.addAttribute("answer", answer);

		return "index";

	}
}

============================== View Page ========================================
src/main/resources/templates/index.html

<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Ask AI</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f7f7f8;
        }

        .chat-container {
            max-width: 800px;
            margin: auto;
            height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .chat-header {
            padding: 15px;
            text-align: center;
            font-weight: bold;
            border-bottom: 1px solid #ddd;
            background: #fff;
        }

        .chat-body {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
            background-color: #f7f7f8;
        }

        .chat-message {
            max-width: 75%;
            padding: 12px 16px;
            border-radius: 12px;
            margin-bottom: 15px;
            line-height: 1.5;
        }

        .user-message {
            background-color: #0d6efd;
            color: white;
            margin-left: auto;
            border-bottom-right-radius: 0;
        }

        .ai-message {
            background-color: #ffffff;
            border: 1px solid #ddd;
            margin-right: auto;
            border-bottom-left-radius: 0;
        }

        .chat-footer {
            padding: 15px;
            border-top: 1px solid #ddd;
            background: #fff;
        }

        .chat-input {
            border-radius: 20px;
        }
    </style>
</head>

<body>

<div class="chat-container">

    <!-- Header -->
    <div class="chat-header">
        Ask AI 
    </div>

    <!-- Chat Body -->
    <div class="chat-body">

        <!-- User Question -->
        <div class="chat-message user-message" th:if="${question}">
            <strong>You:</strong><br>
            <span th:text="${question}"></span>
        </div>

        <!-- AI Answer -->
        <div class="chat-message ai-message" th:if="${answer}">
            <strong>AI:</strong><br>
            <span th:text="${answer}"></span>
        </div>

    </div>

    <!-- Input -->
    <div class="chat-footer">
        <form th:action="@{askAnything}" method="post" class="d-flex gap-2">
            <input type="text" name="question" class="form-control chat-input"
                   placeholder="Message Ask AI..." required>
            <button class="btn btn-primary px-4">Send</button>
        </form>
    </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
================================================================================================

Run the application - http://localhost:8080
Prompt: What is your name and which model are you using?
        What is Java
	What is Python
	Summarize above response in 3 lines / summarize it in 3 lines --- now it is stateless

	Can you please give a computer joke
	Can you create an image of cat holding laptop
	Tell me a joke about Spring Boot


======================
Chat Memory Advisors
======================

=> In Spring AI, by default ChatClient is not remembering our previous conversation with LLM.

=> In Spring AI, Chat Memory Advisors are used to make the AI to remember previous questions and answers in a conversation and respond in the context-aware, just like ChatGPT.

=> A Chat Memory Advisor is a middleware component that

		- store user questions

		- store AI model responses

		- Inject past conversation into every new prompt automatically

=> Spring AI Advisors act as smart middleware that intercept, modify, and enrich requests and responses during interactions with Large Language Models (LLMs). They let you add cross-cutting features like chat memory, RAG, logging, and safety guards without changing your core business logic


==> in service class

	// Making chatclient as statefull
	public OpenAIService(ChatClient.Builder builder, ChatMemory chatMemory) {
		this.chatClient = builder.defaultAdvisors(MessageChatMemoryAdvisor.builder(chatMemory).build()).build();
	}



		return chatClient.prompt()
				 .user(question)
				 .advisors(advisor -> advisor.param(ChatMemory.CONVERSATION_ID, "user-101"))
				 .call()
				 .content();



================================
What are Message Roles in LLMs?
================================

=> When interacting with Large Language Models (LLMs), we send prompts made up of messages. 

=> Each message has a role that helps the LLM understand the context and how to respond.

=> Common Roles (Used by Most APIs) <=
   --------------------------------
1) User		- What the user says or asks. Example: "Tell me a joke."
2) System	- Instructions for how the LLM should behave. Example: "Be formal.", "Be Professional"
3) Assistant	- The LLM's response. Example: "Sure! Why did the chicken..." (Assistant role messages)
4) Function/Tool - Special instructions to run a function or fetch data from our own storage systems (tools) 

Example Prompt Breakdown:

=> System	: "You are a friendly tour guide."
=> User		: "What are the top 3 places to visit in Thailand?"
=> Assistant	: "Sure! The top 3 are..."


Example: a Stage Play

System		: The director giving stage instructions (e.g., "Act like a professional chef")
User		: The audience asking a question (e.g., "How do I cook pasta?")
Assistant	: The actor replying (e.g., "Boil water and add pasta for 8 minutes.")
Function	: A backstage helper fetching ingredients or recipes


### Not All LLM model providers Support All Roles ###

 Provider 		Supported Roles
 --------		---------------
OpenAI 		User, System, Assistant, Function
Anthropic 	User, System, Assistant
Mistral AI 	User, System, Assistant
Google Gemini 	No System/Function roles. Only: User, Model (like Assistant)
 

Note: ** If the System role is not supported (like in Gemini), Spring AI combines the System message into the User message behind the scenes.


=== PRACTICAL ===

-> New Spring Starter Project
   Dependencies: Spring Web, Dev Tools, Open AI

System- 
"""
You are an internal HR assistant. You assist employees with queries related to HR policies
only — such as leave entitlements, working hours, benefits, and code of conduct. 
If a user asks for help with anything outside of these topics,
kindly inform them that you can only assist with queries related to HR policies
"""

System -

"""
 You are an internal IT helpdesk assistant. Your role is to assist 
 employees with IT-related issues such as resetting passwords, 
 unlocking accounts, and answering questions related to IT policies.
 If a user requests help with anything outside of these 
 responsibilities, respond politely and inform them that you are 
 only able to assist with IT support tasks within your defined scope.
"""



@RestController
@RequestMapping(path = "/api")
public class ChatRestController {
	
	private ChatClient chatClient;
	
	public ChatRestController(ChatClient.Builder chatClientBuilder) {
		this.chatClient = chatClientBuilder.build();
	}
	
	@GetMapping(path = "/chat")
	public String handleChatRequest(@RequestParam("question") String question) {
		String responseMessage = chatClient.prompt()
				 		 .system("""
				 		   You are an internal HR assistant. You assist employees with queries 						   	   related to HR policies only — such as leave entitlements, 
						   working hours, benefits, and code of conduct. 
						   If a user asks for help with anything outside of these topics,
						   kindly inform them that you can only assist with queries related 
						   to HR policies
				 		 """)
				 		.user(question)
						.call()
						.content();
		return responseMessage;
	}
}


GET - http://localhost:8080/api/chat?question=Explain Java in 15points   -- here we are restricting the LLM
GET - http://localhost:8080/api/chat?question=How many leaves I can take
GET - http://localhost:8080/api/chat?question=How can I reset my password




=================================
What Are Defaults in Spring AI?
=================================

In Spring AI, defaults refer to pre-configured values or behaviors that are applied automatically to each request made
through the ChatClient — unless overridden.

These defaults are useful when:

 --> You want to avoid repeating the same system message or advisor every time (for each endpoint/ API).
 --> You have a consistent role or tone for your assistant.
 --> You want consistent logging or tool behavior across calls


### Common Defaults You Can Set ###

   defaultSystem(...) - Defines a default system message — which sets the behavior or role of the AI assistant.

   chatClientBuilder.defaultSystem("""
		You are a helpful assistant that answers	//this is a system role message
		HR-related queries.
	""")




defaultAdvisors(...) - Registers advisors like logging or custom filters that
apply to each interaction.
	chatClientBuilder.defaultAdvisors(new SimpleLoggerAdvisor())


defaultTools(...)- Registers tools/functions available to the LLM by default.
	chatClientBuilder.defaultTools(List.of(myToolClassInstance))


defaultOptions(...) - Allows you to set default configuration options for the model request (e.g., temperature, maxTokens, etc.)
	chatClientBuilder.defaultOptions(ChatOptions.builder().temperature(0.3).maxTokens(300).build())


defaultUser(...) - Sets a default user message that will be included in every prompt unless overridden.
	chatClientBuilder.defaultUser("How can you help me ?");



Example:

@RestController
@RequestMapping(path = "/api")
public class ChatRestController {
	
	private ChatClient chatClient;
	
	public ChatRestController(ChatClient.Builder chatClientBuilder) {
		this.chatClient = chatClientBuilder.defaultSystem("""
				 		   You are an internal HR assistant. You assist employees with queries 						   related to HR policies only — such as leave entitlements, 
						   working hours, benefits, and code of conduct. 
						   If a user asks for help with anything outside of these topics,
						   kindly inform them that you can only assist with queries related 
						   to HR policies
					  """)
					.build();
	}
	
	@GetMapping(path = "/chat")
	public String handleChat(@RequestParam("question") String question) {
		String responseMessage = chatClient.prompt()
				 		   .user(question)
						   .call()
						   .content();
		return responseMessage;
	}
}



GET -  http://localhost:8080/api/chat?question=How many leaves I can take
GET -  http://localhost:8080/api/chat?question=What is your name and which model are you using?



Overrinding default system role message

	@GetMapping(path = "/chat")
	public String handleChat(@RequestParam("question") String question) {
		String responseMessage = chatClient.prompt()
				 		 .system("""
				 		    You are an internal IT helpdesk assistant. Your role is to assist 
				 		    employees with IT-related issues such as resetting passwords, 
				 		   unlocking accounts, and answering questions related to IT policies.
				 		    If a user requests help with anything outside of these 
				 		    responsibilities, respond politely and inform them that you are 
				 		  only able to assist with IT support tasks within your defined scope.
				 		 """)
				 		.user(question)
						.call()
						.content();
		return responseMessage;
	}



=== defaultUser() message ====


	public ChatRestController(ChatClient.Builder chatClientBuilder) {
		this.chatClient = chatClientBuilder.defaultSystem("""
				 		   You are an internal HR assistant. You assist employees with queries 						   related to HR policies only — such as leave entitlements, 
						   working hours, benefits, and code of conduct. 
						   If a user asks for help with anything outside of these topics,
						   kindly inform them that you can only assist with queries related 
						   to HR policies
					""")
			****		.defaultUser("How can you help me?")
					.build();
	}



*** Better to create a separate configuration class to create ChatClient bean

com.capgemini.ai.configs.ChatClientConfig

@Configuration
public class ChatClientConfig {
	@Bean
	public ChatClient generateChatClient(ChatClient.Builder chatClientBuilder) {
		return chatClientBuilder.defaultSystem("""
				 		   You are an internal HR assistant. You assist employees with queries 						   related to HR policies only — such as leave entitlements, 
						   working hours, benefits, and code of conduct. 
						   If a user asks for help with anything outside of these topics,
						   kindly inform them that you can only assist with queries related 
						   to HR policies
				""")
				.defaultUser("How can you help me?")
				.build();
	}
}



@RestController
@RequestMapping(path = "/api")
public class ChatRestController {
	
	private ChatClient chatClient;
	
	public ChatRestController(ChatClient chatClient) {
		this.chatClient = chatClient;
	}
....





====================================
Using Prompt Templates in Spring AI
====================================

Why Use Prompt Templates?

 => Simplifies prompt construction
 => Makes prompts reusable and maintainable
 => Keeps logic and text cleanly separated
 => Supports parameterized placeholders (like {customerName})




Usecase: To Get Travel Guidance

------------------------------- Service Class --------------------------------------

@Service
public class OpenAIService {

    // spring ai interface (impl class will be provided by LLM provider)
    private ChatClient chatClient;

    public OpenAIService(ChatClient.Builder builder, ChatMemory chatMemory) {
        this.chatClient = builder.defaultAdvisors(MessageChatMemoryAdvisor.builder(chatMemory).build()).build();
    }
    
    public String askAnything(String question) {
        return chatClient.prompt().user(question).call().content();
    }
    

    public String getTravelGuidance(String place, String month, String language, String budget) {
        
        PromptTemplate promptTemplate = new PromptTemplate("Welcome to the {place} travel guide!\n"
                + "If you're visiting in {month}, here's what you can do:\n" + "1. Must-visit attractions.\n"
                + "2. Local cuisine you must try.\n" + "3. Useful phrases in {language}.\n"
                + "4. Tips for traveling on a {budget} budget.\n" + "Enjoy your trip!");
        
        Prompt prompt = 
                promptTemplate.create(Map.of("place", place, "month", month, "language", language, "budget", budget));
        
        return chatClient.prompt(prompt).call().chatResponse().getResult().getOutput().getText();
    }
}


->  PromptTemplate is a component in Spring AI used to construct structured, dynamic prompts by injecting runtime variables into predefined text placeholders. It uses the AntLR StringTemplate engine under the hood, which allows you to define placeholders wrapped in curly braces like {variableName}

-------------------------------------TravelGuidanceController-----------------------------------

@Controller
public class TravelGuidanceController {

    @Autowired
    private OpenAIService openAiService;

    @GetMapping("/showTravelGuide")
    public String showTravelGuide() {
        return "travelGuide";
    }
    
    @PostMapping("/showTravelGuide")
    public String getTravelGuideResponse(@RequestParam("place") String place, 
                                        @RequestParam("month") String month, 
                                        @RequestParam("language") String language, 
                                        @RequestParam("budget")String budget,
                                        Model model) {
        
        String response = openAiService.getTravelGuidance(place, month, language, budget);
        
        model.addAttribute("place", place);
        model.addAttribute("response", response);
        
        return "travelGuide";
        
    }
}


---------------------------------UI Page----------------------------------------------      
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>AI Travel Guidance</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Marked.js (Markdown Parser) -->
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
</head>

<body class="bg-light">

<nav class="navbar navbar-dark bg-primary">
    <div class="container-fluid">
        <span class="navbar-brand mb-0 h1">
            <i class="bi bi-airplane"></i> AI Travel Guide
        </span>
    </div>
</nav>

<div class="container mt-5">

    <div class="row justify-content-center">
        <div class="col-md-8">

            <!-- Input Card -->
            <div class="card shadow-lg border-0">
                <div class="card-header bg-primary text-white text-center">
                    <h4><i class="bi bi-globe"></i> Plan Your Trip</h4>
                </div>

                <div class="card-body">
                    <form th:action="@{/showTravelGuide}" method="post">

                        <div class="mb-3">
                            <label class="form-label">Destination</label>
                            <input type="text" class="form-control" name="place" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Travel Month</label>
                            <input type="text" class="form-control" name="month" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Language</label>
                            <select class="form-select" name="language">
                                <option>English</option>
                                <option>Hindi</option>
                                <option>Telugu</option>
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Budget</label>
                            <input type="text" class="form-control" name="budget" required>
                        </div>

                        <div class="d-grid">
                            <button class="btn btn-primary btn-lg">
                                <i class="bi bi-search"></i> Get Travel Guide
                            </button>
                        </div>

                    </form>
                </div>
            </div>

            <!-- Response Card -->
            <div class="card shadow mt-4" th:if="${response}">
                <div class="card-header bg-success text-white">
                    <h5>
                        <i class="bi bi-info-circle"></i>
                        Travel Guide for <span th:text="${place}"></span>
                    </h5>
                </div>

                <div class="card-body">
                    <!-- Raw markdown (hidden) -->
                    <textarea id="markdownText" class="d-none"
                              th:text="${response}"></textarea>

                    <!-- Rendered HTML -->
                    <div id="markdownOutput" class="markdown-body"></div>
                </div>
            </div>

        </div>
    </div>

</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<!-- Markdown Rendering Script -->
<script>
    document.addEventListener("DOMContentLoaded", function () {
        const markdownText = document.getElementById("markdownText");
        const outputDiv = document.getElementById("markdownOutput");

        if (markdownText && outputDiv) {
            const markdown = markdownText.value;
            outputDiv.innerHTML = marked.parse(markdown);
        }
    });
</script>

</body>
</html>


http://localhost:8080/showTravelGuide

Destination : Thailand
Travel Month : February
Language : English
Budget : 50000


Try in ChatGPT

Welcome to the Thailand travel guide!. 
If you're visiting in February, here's what you can do: 
1. Must-visit attractions
2. Local cuisine you must try
3. Useful phrases in English
4. Tips for traveling on a 200000 budget.
Enjoy your trip!



** Scenario #2 **


-- Controller --

@RestController
@RequestMapping("/api")
public class PromptTemplateController {

    private final ChatClient chatClient;

    public PromptTemplateController(ChatClient chatClient) {
        this.chatClient = chatClient;
    }

    String promptTemplate = """
       A customer named {customerName} sent the following message:
               "{customerMessage}"

	Write a polite and helpful email response addressing the issue.
	Maintain a professional tone and provide reassurance.

	Respond as if you're writing the email body only. Don't include subject,
signature
     """

    //@Value("classpath:/promptTemplates/userPromptTemplate.st")
    //Resource userPromptTemplate;

    @GetMapping("/email")
    public String emailResponse(@RequestParam("customerName") String customerName,
            @RequestParam("customerMessage") String customerMessage) {
        return chatClient
                .prompt()
                .system("""
                        You are a professional customer service assistant which helps drafting email
                        responses to improve the productivity of the customer support team
                        """)
                .user(promptTemplateSpec ->
                        promptTemplateSpec.text(userPromptTemplate) //(promptTemplate)
                                .param("customerName", customerName)
                                .param("customerMessage", customerMessage))
                .call().content();
    }
}


GET - http://localhost:8080/api/email?customerName=Sanjay&customerMessage=I am not able to access my account. It says account is blocked. Please Help


### Better approach ###

Where can Prompt Templates Live ?
  You can store them in:  src/main/resources/promptTemplates/userPromptTemplate.st
  File extension: .st (String Template)

--- userPromptTemplate.st --
A customer named {customerName} sent the following message:
               "{customerMessage}"

Write a polite and helpful email response addressing the issue.
Maintain a professional tone and provide reassurance.

Respond as if you're writing the email body only. Don't include subject,
signature


How to Use in Code?
 @Value("classpath:/promptTemplates/userPromptTemplate.st")
 Resource userPromptTemplate;



=== Use with .user() Prompt Builder ===
chatClient.prompt()
	  .user(promptUserSpec -> promptUserSpec.text(userPromptTemplate)
					.param("customerName", customerName)
					.param("customerMessage", customerMessage))
		.call().content();




@RestController
@RequestMapping("/api")
public class PromptTemplateController {

    private final ChatClient chatClient;

    public PromptTemplateController(ChatClient chatClient) {
        this.chatClient = chatClient;
    }

    @Value("classpath:/promptTemplates/userPromptTemplate.st")
    Resource userPromptTemplate;

    @GetMapping("/email")
    public String emailResponse(@RequestParam("customerName") String customerName,
            @RequestParam("customerMessage") String customerMessage) {
        return chatClient
                .prompt()
                .system("""
                        You are a professional customer service assistant which helps drafting email
                        responses to improve the productivity of the customer support team
                        """)
                .user(promptTemplateSpec ->
                        promptTemplateSpec.text(userPromptTemplate)
                                .param("customerName", customerName)
                                .param("customerMessage", customerMessage))
                .call().content();
    }

}





===============
Prompt Stuffing
===============

Prompt Stuffing = Giving the LLM an open book before answering a question.

• You include contextual data or reference text along with the user’s question.
• The LLM uses this extra content to answer the question accurately — even if it was not pre-trained on the topic.
• This technique is also known as in-context learning or retrieval-augmented prompting (when done programmatically).

Example: Internal company policy lookup
Prompt (Stuffed):
 "According to the company's HR policy, employees are
  eligible for 18 days of paid leave annually. Unused leave
  can be carried over to the next year."

Question:
How many paid leaves do employees get each year?

LLM Answer:
Employees are eligible for 18 days of paid leave annually.




--- src/main/resources/promptTemplates/systemPromptTemplate.st ---

You are an internal HR assistant. You assist employees with queries related to HR policies
only — such as leave entitlements, working hours, benefits, and code of conduct.
HR Policy Summary:
1. 18 days of paid leave annually
2. Up to 8 unused leave days can be carried over to the next year
3. Standard working hours: 9 AM to 6 PM, Monday to Friday
4. Notice period - 30 days
5. Maternity leaves - 6 months
6. Paternity leaves - 2 weeks
7. National holidays are company-wide off days
8. Benefits include health insurance, provident fund, and annual health checkup
9. Employees must adhere to professional behavior, punctuality, and data confidentiality

Politely inform users that you can only help with HR policy-related topics if they ask
something outside your scope.


==> New Controller: PromptStuffingController

@RestController
@RequestMapping("/api")
public class PromptStuffingController {

    private final ChatClient chatClient;

    public PromptStuffingController(ChatClient chatClient) {
        this.chatClient = chatClient;
    }

    @Value("classpath:/promptTemplates/systemPromptTemplate.st")
    Resource systemPromptTemplate;

    @GetMapping("/prompt-stuffing")
    public String promptStuffing(@RequestParam("message") String message) {
        return chatClient
                .prompt()
                .system(systemPromptTemplate)
                .user(message)
                .call()
                .content();
    }

}



GET - http://localhost:8080/api/prompt-stuffing?message=I used 5 leaves in this year. How many will be forwarded to next year



-----------------------------------------------------------------------
Assignment : Develop Image generation functionality using Spring AI
-----------------------------------------------------------------------        

# Create Image Service
import org.springframework.ai.image.ImageModel;
import org.springframework.ai.image.ImagePrompt;
import org.springframework.ai.image.ImageResponse;
import org.springframework.stereotype.Service;

@Service
public class ImageGenerationService {

    private final ImageModel imageModel;

    public ImageGenerationService(ImageModel imageModel) {
        this.imageModel = imageModel;
    }

    public String generateImage(String prompt) {

        ImageResponse response =
                imageModel.call(new ImagePrompt(prompt));

        return response.getResult()
                       .getOutput()
                       .getUrl();
    }
}


# Request DTO
public record ImageRequest(String prompt) {
}


# Rest Controller
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/images")
public class ImageController {

    private final ImageGenerationService imageService;

    public ImageController(ImageGenerationService imageService) {
        this.imageService = imageService;
    }

    @PostMapping("/generate")
    public String generate(@RequestBody ImageRequest request) {
        return imageService.generateImage(request.prompt());
    }
}

# Example Request

POST /api/images/generate
Content-Type: application/json

{
    "prompt": "A futuristic smart city at sunset, ultra realistic"
}


# Returning JSON
public record ImageResponseDto(String imageUrl) {}

# Controller
@PostMapping("/generate")
public ImageResponseDto generate(@RequestBody ImageRequest request) {

    String url = imageService.generateImage(request.prompt());

    return new ImageResponseDto(url);
}


# Custom Image Options

import org.springframework.ai.image.ImageOptions;
import org.springframework.ai.image.ImagePrompt;

ImageOptions options = ImageOptions.builder()
        .width(1024)
        .height(1024)
        .build();

ImagePrompt prompt = new ImagePrompt(
        "A cyberpunk city at night",
        options
);

ImageResponse response = imageModel.call(prompt);




====================================================================
Understand about Tokens

https://developers.openai.com/api/docs/models	- GPT-4.1

https://platform.openai.com/tokenizer

Enter some text: Which city is the capital of India ?
		 Raining is nice. What do you think about it?

 


